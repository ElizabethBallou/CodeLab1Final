using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TeacupList : MonoBehaviour
{

    private GameObject[] TeacupTypes;
    public int TeacupIndex;
    
    // Start is called before the first frame update
    void Start()
    {

        //ObjectList.Add(???) //create a new teacup and add it to the list

        
        //ObjectList[??].transform.position = new Vector3(???, -3, 0); //set the position of the last gem to be based on their position
        
    }

    // Update is called once per frame
    void Update()
    {
    }
}
